version: "3.9"

services:
  backend:
    build: ./backend
    container_name: pricedesk-backend
    environment:
      - DB_PATH=/data/pricedesk.db
    volumes:
      - ./data:/data
    ports:
      - "5000:5000"
    restart: unless-stopped
